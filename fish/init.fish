#!/usr/bin/env fish

# Run this file only once when bootstrapping a new
# machine.

set -U EDITOR vim
set -U GREP_OPTIONS --color=auto
set -U PATH /opt/bin /usr/local/sbin /usr/local/bin /usr/sbin /usr/bin /sbin /bin ~/.dotfiles/bin
