# Don't display the copyright page.
startup_message off

# Set the escape sequence to just a single backtick instead of '^a'.
escape ``

# Toggle between last used windows.
bind ' ' other

# Shorten the time a message is displayed.
msgwait 0.5

# Set up UTF-8 as the default encoding.
encoding UTF-8

# Set format for activity messages.
activity "Activity in window %n %t at %c"

# This will show the titles and locations of all screen windows.
hardstatus alwayslastline "%{KW} %-w%{Wg} %n %t %{KW}%+w %=%l"

# Increase the scroll size.
defscrollback 3000

# Copy to clipboard.
bind b eval "writebuf /tmp/screen-pbcopy" "exec bash -c '/usr/bin/pbcopy < /tmp/screen-pbcopy'"

screen -t server 0
screen -t vi 1
screen -t test 2
screen -t irb 3
